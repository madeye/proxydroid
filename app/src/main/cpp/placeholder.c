/* Placeholder so the CMake externalNativeBuild target has at least one
 * source file. All real native code now lives in the Rust tun2socks
 * crate (built by the rust-android-gradle plugin). This file may be
 * removed once externalNativeBuild is dropped from app/build.gradle.
 */
static int proxydroid_native_placeholder(void) { return 0; }
